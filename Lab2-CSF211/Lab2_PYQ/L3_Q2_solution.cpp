#include<bits/stdc++.h>
using namespace std;

// predicate function returns TTTTTTTFFFFFFFF => we find the last occurence of T
bool canRun(vector<int>&batteries, int n, long long x) {
        // can we run the n computers for x minutes ??
        
}

long long solve(int n, int m, vector<int>& A) {
    int 
}