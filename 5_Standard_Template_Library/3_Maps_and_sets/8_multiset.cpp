#include<bits/stdc++.h>
using namespace std;

/*
    to enter a element use .insert() and to delete a particular data use .erase() to remove all the data stored use .clear()
*/

int main(){

    multiset<string> s;
    // similar to sets operations are O(log(n));
    s.insert("abcd"); // O(log(n))
    s.insert("bcd");
    s.insert("abcd");
    // allows multiple insertions of same input 

}