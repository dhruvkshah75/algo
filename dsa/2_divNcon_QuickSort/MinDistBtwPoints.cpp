#include <bits/stdc++.h>
using namespace std;

// question link:

// 3rd HARD question 

class Solution {
public:
    double minDist(vector<vector<int>>&points) {

    } 
};


int main(){
    int n;
    cin >> n;
    vector<vector<int>> points(n, vector<int>(2));
    for(int i=0; i<n; i++) {
        cin >> points[i][0] >> points[i][1];
    }

    Solution s = Solution();
}