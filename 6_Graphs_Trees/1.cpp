#include<bits/stdc++.h>

using namespace std;
typedef long long ll;
typedef unsigned long long ull;

/*
    if a tree has n nodes then it will have n-1 edges 
    trees are graphs with no cycles in it 

    connected components of graphs
    if the edges are given a direction then it is called a directed graphs
    
*/

int main(){
     
}