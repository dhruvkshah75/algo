using namespace std;

// NOTE: You might see an error in VS Code saying "ListNode" is undefined.
// This is NOT an issue and can be ignored.

ListNode* solve(ListNode* head, int k) {
    // we make group of k lists and then reverse them 
}
